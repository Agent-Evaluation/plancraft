# AGENTS.md

## Setup commands
- Always use `uv` for python so basically `uv run <command>`
- Always use `uv sync` to install dependencies
- Always use `uv add <package>` to add dependencies
- Always use `uv pip install <package>` to install dependencies

## Save any markdown and images for documentation generated in the `docs` folder here: `/Users/abhi/Thesis/plancraft/plancraft/agents/docs`